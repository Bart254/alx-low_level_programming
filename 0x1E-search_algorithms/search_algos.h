#ifndef SEARCH_ALGOS
#define SEARCH_ALGOS
#include <stdlib.h>
int linear_search(int *array, size_t size, int value);
void search_array(int low, int high);
int binary_search(int *array, size_t size, int value);

#endif/*end header enclosement*/
