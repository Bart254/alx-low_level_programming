#include <stdio.h>

/**
 * main - prints the program's name
 * @argc: number of arguments passed
 * @argv:  the vector
 * Return: 0 (successful)
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
