#include <ctype.h>
/**
* _islower - Prints lower for lowercase
* Return: 1 if lowercase 0 if not
* @c: argumentcat
*/
int _islower(int c)
{
	if (c > 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
