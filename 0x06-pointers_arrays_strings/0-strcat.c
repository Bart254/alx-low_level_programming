#include <string.h>

/**
 * _strcat - This program concatenates strings
 * @dest: Where the string is added to
 * @src: The string being added
 * Return: The final concatenated strings
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}

