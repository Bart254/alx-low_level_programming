#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: First string
 * @s2: Second string
 * Return: less than zero, zero or greater than zero int
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
