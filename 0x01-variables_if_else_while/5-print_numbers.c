#include <stdio.h>
/**
* main - Entry point
*This program prints all numbers from 0 to 9
* Return: Always 0 (Success)
*/
int main(void)
{
int digit = 0;

while (digit < 10)
{printf("%d", digit);
digit++;
}
return (0);
}
